# ---------------------------------------------------------------------------
# Axial Screening Assistant - runtime dependencies
#
# PyTorch is deliberately NOT pinned here. Install it first from the CUDA
# index so the GPU build is selected, then install this file:
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
#   pip install -r requirements.txt
#
# See docs/SETUP.md for the full provisioning sequence.
# ---------------------------------------------------------------------------

# --- Numerics and imaging -------------------------------------------------
numpy>=1.26,<3
scipy>=1.11
opencv-python-headless>=4.9          # CLAHE, resizing, colour-space ops
Pillow>=10.2
scikit-image>=0.22
pydicom>=2.4                         # Grande CD ingestion (DICOM)
nibabel>=5.2                         # NIfTI ingestion (contingency datasets)

# --- Machine learning -----------------------------------------------------
scikit-learn>=1.4                    # metrics, stratified splits, k-fold
pandas>=2.2
tabulate>=0.9                        # pandas.DataFrame.to_markdown backend
matplotlib>=3.8
seaborn>=0.13
tqdm>=4.66

# --- RAG stack ------------------------------------------------------------
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langchain-huggingface>=0.1.0
langchain-ollama>=0.2.0
langchain-text-splitters>=0.3.0
sentence-transformers>=3.0           # multilingual embeddings (EN + Nepali)
faiss-cpu>=1.8                       # vector index; CPU build is ample at this corpus size
ollama>=0.3.0                        # local LLM client (Llama 3.1 / Qwen2.5)

# --- Web application ------------------------------------------------------
Flask>=3.0
Flask-WTF>=1.2                       # CSRF protection on upload + chat forms
Werkzeug>=3.0
python-dotenv>=1.0
gunicorn>=21.2; sys_platform != "win32"
waitress>=3.0; sys_platform == "win32"

# --- Reporting ------------------------------------------------------------
reportlab>=4.1                       # PDF report generation
fpdf2>=2.7                           # Devanagari-capable fallback writer

# --- Configuration and validation ----------------------------------------
pydantic>=2.6
PyYAML>=6.0

# --- Developer tooling ----------------------------------------------------
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4
